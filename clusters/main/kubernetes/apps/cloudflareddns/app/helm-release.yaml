apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
    name: cloudflareddns
    namespace: cloudflareddns
spec:
    interval: 15m
    chart:
        spec:
            chart: cloudflareddns
            version: 13.0.6
            sourceRef:
                kind: HelmRepository
                name: truecharts
                namespace: flux-system
    releaseName: cloudflareddns
    values:
        # for now until image is fixed
        image:
            repository: ghcr.io/hotio/cloudflareddns
            pullPolicy: IfNotPresent
            tag: latest@sha256:3017a1a0cddacd8d7c0b6c8fa22ef39940228350b5e91e569cd4826a4c586908
        securityContext:
            container:
                runAsNonRoot: false
                readOnlyRootFilesystem: false
                runAsUser: 0
                runAsGroup: 0
        TZ: Europe/Berlin
        cloudflareddns:
            api_token: ${CF_DDNS_API_TOKEN}
            host_zone_record:
                - domain: ${CF_DDNS_DOMAIN}
                  record: A
                  zone: ${CF_DDNS_ZONE}
        credentials:
            aws:
                type: s3
                accessKey: ${AWS_ACCESS_KEY}
                bucket: ${AWS_BUCKET}
                encrKey: ${AWS_ENCR_KEY}
                secretKey: ${AWS_SECRET_KEY}
                url: ${AWS_URL}
        persistence:
            config:
                volsync:
                    - credentials: aws
                      name: cloudflareddns-config
                      src:
                          enabled: true
                      dest:
                          enabled: true
                      type: restic
        release_name: cloudflareddns
